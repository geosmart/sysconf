[core]
	quotePath = false
[color]
	ui = true
[diff]
    algorithm = histogram
[pull]
	rebase = true
[push]
    default = upstream
[user]
    email = geosmart@foxmail.com
    name = geosmart
[alias]
    co = checkout
    dc = diff --cached
    df = diff
    rb = rebase
    rbi = rebase -i HEAD~10
    rh = reset HEAD
    st = status
